<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_menu_home"
            android:title="@string/menu_home" />
        <item
            android:id="@+id/nav_top_up_history"
            android:icon="@drawable/ic_menu_topup_history"
            android:title="Top Up History" />
        <item
            android:id="@+id/nav_payment_history"
            android:icon="@drawable/ic_menu_payment_history"
            android:title="Payment History" />
        <item
            android:id="@+id/nav_logout"
            android:icon="@drawable/ic_menu_logout"
            android:title="Logoff"
            android:onClick="onLogoutClickListener"/>
    </group>

</menu>